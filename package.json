{
  "name": "sinc-liquidity-pipeline",
  "version": "1.0.0",
  "description": "SINC token liquidity and compliance pipeline",
  "type": "module",
  "scripts": {
    "deploy": "hardhat run scripts/deploy-simple.js --network base"
  },
  "devDependencies": {
    "@chainlink/contracts": "^1.3.0",
    "@openzeppelin/contracts": "^5.0.0",
    "ethers": "^6.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.0.0"
  }
}